/** Demo user created by `npm run db:seed` — safe to reference in docs and dev UI. */
export const DEMO_ACCOUNT = {
  email: "demo@gameshelf.dev",
  password: "demo-password",
  name: "Demo Player",
} as const;
