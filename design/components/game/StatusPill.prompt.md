Small offset-shadow status chip mapped to the Prisma `GameStatus` enum. **Intentional addition** — no backlog screen ships upstream yet, so this is inferred from the schema + visual language, not copied from a real screen.

```jsx
<StatusPill status="PLAYING" />
```
