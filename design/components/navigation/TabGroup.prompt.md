Underline-free two-tab switcher (Sign In / Sign Up) with a gold active state, sat directly on the card's border.

```jsx
<TabGroup tabs={[{value:'login',label:'Sign In'},{value:'register',label:'Sign Up'}]} active={tab} onChange={setTab} />
```
