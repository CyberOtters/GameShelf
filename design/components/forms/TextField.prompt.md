Uppercase-label text input; lifts with a teal offset shadow on focus, matching the button's press language.

```jsx
<TextField id="email" label="Email" type="email" value={email} onChange={setEmail} />
```
